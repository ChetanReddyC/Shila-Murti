apiVersion: batch/v1
kind: CronJob
metadata:
  name: scale-up-cronjob
  namespace: default
spec:
  schedule: "0 8 * * *"  # 8 AM IST
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: kubectl
            image: bitnami/kubectl
            command:
            - /bin/sh
            - -c
            - |
              kubectl scale deployment shilamurti-deploy --replicas=2 -n shilamurti-frontend
              kubectl scale deployment shilamurti-deploy-backend --replicas=2 -n shilamurti-backend
          restartPolicy: OnFailure
